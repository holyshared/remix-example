{
  "name": "remix-example",
  "workspaces": [
    "packages/client",
    "packages/server"
  ],
  "devDependencies": {
    "typescript": "^5.1.6"
  },
  "scripts": {
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server"
  }
}
